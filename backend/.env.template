# ─── Local LLM ────────────────────────────────────────────────────────────────
# Ollama    → http://localhost:11434/v1   |  LLM_API_KEY=ollama
# LM Studio → http://localhost:1234/v1   |  LLM_API_KEY=lmstudio
# Used for the optional `--categorize` and `--match-ingredients` passes only —
# any instruction-following (non-vision) model works, e.g. qwen3.5:9b.
LLM_BASE_URL=http://localhost:11434/v1
LLM_API_KEY=ollama
LLM_MODEL=qwen3.5:9b
LLM_TIMEOUT=120
LLM_MAX_RETRIES=3
