# Core settings / validation
requests==2.32.3
python-dotenv==1.0.1
pydantic==2.7.4
pydantic-settings==2.3.4

# Local LLM connector (OpenAI-compatible API) — used for --categorize and
# --match-ingredients only, no vision model needed anymore.
# httpx is pinned explicitly: 0.28+ dropped the `proxies` kwarg openai's client
# still passes internally, which breaks client construction entirely.
openai==1.54.4
httpx==0.27.2

# Robust JSON repair for LLM output quirks (markdown fences, trailing commas, etc.)
json-repair==0.28.4

# Headless-browser DOM reads of supermarktscanner.nl (server-rendered, but
# still needs a real browser context to fetch reliably) — run
# `playwright install chromium` once after installing.
playwright==1.61.0
